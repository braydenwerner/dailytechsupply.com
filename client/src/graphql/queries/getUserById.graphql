query getUserById($uid: String!) {
  getUserById(uid: $uid) {
    id
    uid
    display_name
    email
    about
    profile_picture_url
    last_updated_password
    updated_at
    created_at
  }
}
