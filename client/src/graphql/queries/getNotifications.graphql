query getNotifications($num_notifications: Float!) {
  getNotifications(num_notifications: $num_notifications) {
    notifications {
      title
      text
      is_read
      item_link
      created_at
    }
    gotLastNotification
  }
}
